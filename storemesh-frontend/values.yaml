replicaCount: 2

image:
  repository: ghcr.io/sartim/storemesh-frontend
  tag: "0.1.1"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3000

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
