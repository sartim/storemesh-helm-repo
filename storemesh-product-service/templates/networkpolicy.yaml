{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: storemesh-product-service
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: storemesh-product-service
  policyTypes: [Ingress]
  ingress:
    - from:
{{- range .Values.networkPolicy.allowedIngressNamespaces }}
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ . | quote }}
{{- end }}
      ports:
        - protocol: TCP
          port: 50051
{{- end }}
