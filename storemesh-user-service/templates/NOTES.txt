The StoreMesh user service has been deployed.

HTTP service:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "storemesh-user-service.fullname" . }} 8080:{{ .Values.service.httpPort }}

gRPC service DNS:
  {{ include "storemesh-user-service.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.grpcPort }}

The chart expects a Kubernetes Secret named {{ include "storemesh-user-service.secretName" . }}
with DATABASE_URL, REDIS_URL, and JWT_SECRET keys. Use an external secret provider for
production deployments; do not commit secret values to Git.
