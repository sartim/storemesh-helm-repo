{{- if .Values.prometheusRule.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: storemesh-inventory-service
  labels: {{- toYaml .Values.prometheusRule.additionalLabels | nindent 4 }}
spec:
  groups:
    - name: storemesh-inventory-service
      rules:
        - alert: StoreMeshInventoryServiceUnavailable
          expr: kube_deployment_status_replicas_available{namespace="{{ .Release.Namespace }}",deployment="storemesh-inventory-service"} < 1
          for: 10m
          labels: {severity: critical}
          annotations: {summary: StoreMesh Inventory Service has no available replicas}
{{- end }}
